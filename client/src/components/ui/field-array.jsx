// Field array utilities for form handling
export const useFieldArray = ({ control, name }) => {
  const fields = [];
  const append = (value) => {};
  const remove = (index) => {};
  
  return { fields, append, remove };
};